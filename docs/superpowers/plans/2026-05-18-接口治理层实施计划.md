# 接口治理层实施计划

> **给执行型 agent 使用：** 必须逐项执行，并使用复选框追踪状态。

**目标：** 统一 `users`、`malls`、`accounts` 三个模块的列表接口约定，补齐审计日志、中文异常语义和接口文档，形成下一次稳定提交点。

**架构：** 保持现有 `controller -> service -> model` 分层，在工具层新增分页归一化和审计日志能力。控制器负责接参和响应，服务层负责查询条件，日志通过共享工具输出。

**技术栈：** Node.js、Koa、Sequelize、MySQL、log4js、node:test

---

### 任务 1：新增统一分页工具

**文件：**
- 新建：`backend/utils/pagination.js`
- 修改：`backend/controllers/user.js`
- 修改：`backend/controllers/mall.js`
- 修改：`backend/controllers/account.js`

- [ ] 新建统一分页参数解析与列表结果格式化工具
- [ ] 将三个控制器的列表参数改为优先读取 `keyword`
- [ ] 保留 `name` / `username` 兼容逻辑
- [ ] 列表返回统一补上 `page` 和 `pageSize`

### 任务 2：统一服务层筛选规则

**文件：**
- 修改：`backend/services/user.js`
- 修改：`backend/services/mall.js`
- 修改：`backend/services/account.js`

- [ ] `users` 服务支持通过 `keyword` 搜索 `name`
- [ ] `malls` 服务支持通过 `keyword` 搜索 `name` 和 `category`
- [ ] `accounts` 服务支持通过 `keyword` 搜索 `username`

### 任务 3：新增审计日志工具并接入 CRUD

**文件：**
- 新建：`backend/utils/auditLog.js`
- 修改：`backend/utils/log4js.js`
- 修改：`backend/controllers/user.js`
- 修改：`backend/controllers/mall.js`
- 修改：`backend/controllers/account.js`

- [ ] 扩展 `log4js`，增加 `audit` 分类
- [ ] 新建审计日志工具，统一写入操作者、模块、动作、目标和结果
- [ ] 接入 `users`、`malls`、`accounts` 的新增、修改、删除、重置密码流程

### 任务 4：统一中文异常语义

**文件：**
- 修改：`backend/utils/userValidation.js`
- 修改：`backend/utils/mallValidation.js`
- 修改：`backend/utils/accountValidation.js`
- 修改：`backend/controllers/user.js`
- 修改：`backend/controllers/mall.js`
- 修改：`backend/controllers/account.js`
- 视情况修改：`backend/middleware/auth.js`

- [ ] 将本次涉及模块的校验消息改为清晰中文
- [ ] 将资源不存在等控制器提示改为清晰中文
- [ ] 保持状态码语义与现有中间件兼容

### 任务 5：补齐接口测试

**文件：**
- 新建：`backend/tests/user.test.js`
- 新建：`backend/tests/mall.test.js`
- 修改：`backend/tests/account.test.js`
- 修改：`backend/package.json`

- [ ] 为 `users` 新增列表、创建、参数错误测试
- [ ] 为 `malls` 新增列表、创建、参数错误测试
- [ ] 为 `accounts` 新增 `keyword/page/pageSize` 统一规则测试
- [ ] 更新测试脚本，纳入新测试文件

### 任务 6：补充中文接口文档

**文件：**
- 新建：`backend/docs/API接口说明.md`
- 视情况修改：`backend/README.md`

- [ ] 编写鉴权方式与通用响应结构说明
- [ ] 编写 `users`、`malls`、`accounts` 的接口说明和示例
- [ ] 编写统一分页筛选规则和常见错误码说明

### 任务 7：验证并整理提交

**文件：**
- 无新增代码文件

- [ ] 运行 `npm test`
- [ ] 确认新增测试通过
- [ ] 整理本次提交说明为中文
